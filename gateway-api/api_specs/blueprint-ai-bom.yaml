# Blueprint AI BOM API Specification
# Human-in-the-Loop 기반 도면 분석 및 BOM 생성
# BlueprintFlow 통합 스펙

apiVersion: v1
kind: APISpec

metadata:
  id: blueprint-ai-bom
  name: Blueprint AI BOM
  version: 3.0.0
  port: 5020
  host: blueprint-ai-bom-backend  # Docker 서비스명
  description: AI 기반 도면 분석 및 BOM 생성. Human-in-the-Loop 검증 UI 제공.
  author: AX Team
  tags:
    - bom
    - detection
    - verification
    - human-in-the-loop
    - export

server:
  endpoint: /detection/{session_id}/detect
  method: POST
  contentType: application/json
  timeout: 120
  healthEndpoint: /health

blueprintflow:
  category: analysis
  color: "#8b5cf6"
  icon: FileSpreadsheet
  requiresImage: true
  interactiveMode: true  # Human-in-the-Loop UI 필요
  uiUrl: "http://localhost:3000"  # Frontend URL
  recommendedInputs:
    - from: yolo
      field: detections
      reason: YOLO 검출 결과를 BOM 검증 입력으로 사용
    - from: imageinput
      field: image
      reason: 원본 도면 이미지

inputs:
  - name: image
    type: Image
    required: true
    description: 분석할 도면 이미지
  - name: detections
    type: DetectionResult[]
    required: true
    description: YOLO 노드의 검출 결과 (필수 - YOLO 노드 연결 필요)

outputs:
  - name: bom_data
    type: BOMData
    description: 생성된 BOM 데이터 (품목별 수량, 단가, 합계)
  - name: items
    type: BOMItem[]
    description: BOM 항목 목록
  - name: summary
    type: BOMSummary
    description: BOM 요약 (총 수량, 소계, 부가세, 합계)
  - name: approved_count
    type: number
    description: 승인된 검출 수
  - name: export_url
    type: string
    description: BOM 다운로드 URL

parameters: []
  # 파라미터 없음 - 세션 생성 후 검증 UI에서 모든 작업 수행

mappings:
  input:
    image: image
    detections: detections
  output:
    bom_data: bom_data
    items: bom_data.items
    summary: bom_data.summary
    approved_count: approved_count
    export_url: export_url

i18n:
  ko:
    label: Blueprint AI BOM
    description: YOLO 검출 결과로 BOM 세션을 생성합니다. 검증 UI에서 확인 후 BOM 생성.
  en:
    label: Blueprint AI BOM
    description: Create BOM session from YOLO detections. Review in verification UI then generate BOM.

examples:
  - 도면 이미지 → YOLO 검출 → Blueprint AI BOM → 검증 UI → BOM 생성

usageTips:
  - "⭐ YOLO 노드 연결 필수"
  - "💡 세션 생성 후 검증 UI(localhost:3000)에서 BOM 생성"
  - "27개 전기 패널 부품 클래스 지원"

# 지원 클래스 목록
supportedClasses:
  - beam
  - bolt
  - channel
  - clamp
  - elbow
  - flange
  - gasket
  - hbeam
  - ibeam
  - nut
  - pipe
  - plate
  - pump
  - reducer
  - tank
  - tee
  - union
  - valve
  - vessel
  - washer
  - weldneck
  - cap
  - coupling
  - cross
  - expansion_joint
  - filter
  - strainer

resources:
  cpu:
    ram: "~512MB"
    minRam: 256
    cores: 2
    note: "세션 관리 및 BOM 생성만 수행 (검출은 YOLO 노드에서)"
