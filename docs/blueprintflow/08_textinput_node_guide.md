# TextInput Node - 사용 가이드

## 📌 개요

**TextInput 노드**는 사용자가 직접 텍스트를 입력하여 Vision-Language 모델이나 향후 추가될 텍스트 기반 API로 전달하기 위한 노드입니다.

**카테고리**: Input Node
**아이콘**: Type (📝)
**색상**: Purple (#8b5cf6)

---

## 🎯 주요 용도

### 1. VL (Vision-Language) API와 함께 사용

VL 모델에 이미지와 함께 **질문이나 프롬프트**를 전달할 때 사용합니다.

```
[ImageInput] ─┬─→ [VL Model]
              │     ↑
[TextInput] ──┘     │
   "이 도면의        질문 입력
   치수를 추출해줘"
```

**사용 시나리오**:
- "이 도면에서 용접 기호를 찾아줘"
- "이미지에 있는 한글 텍스트를 모두 추출해줘"
- "이 부품의 재질은 무엇인가요?"
- "치수 공차 정보를 분석해줘"

### 2. 향후 확장: Text-to-Image API

미래에 Text-to-Image 생성 API가 추가될 경우 사용합니다.

```
[TextInput] ──→ [Text-to-Image API]
   "기계 부품
   도면 생성"        → 생성된 이미지
```

### 3. 향후 확장: LLM 기반 분석 API

GPT, Claude 같은 LLM API와 연동 시 사용합니다.

```
[ImageInput] ─┬─→ [OCR API] ──→ [LLM API] ──→ 분석 결과
              │                    ↑
[TextInput] ──┘                    │
   "공차 분석 요청"                  명령어 입력
```

---

## 📖 파라미터

| 파라미터 | 타입 | 기본값 | 설명 |
|---------|------|--------|------|
| `text` | string | `""` | 입력할 텍스트 (최대 10,000자) |

---

## 🔌 입력/출력

### 입력
없음 (시작 노드)

### 출력

| 출력명 | 타입 | 설명 |
|-------|------|------|
| `text` | string | 사용자가 입력한 텍스트 |
| `length` | number | 텍스트 길이 (문자 수) |

---

## 💡 사용 예시

### 예시 1: VL 모델에 질문 전달

**워크플로우**:
```
┌──────────────┐
│ ImageInput   │ (도면 이미지 업로드)
└──────┬───────┘
       │ image
       ↓
┌──────────────┐
│ TextInput    │ text: "이 도면의 모든 치수를 추출해주세요"
└──────┬───────┘
       │ text
       ↓
┌──────────────┐
│ VL Model     │
│              │
│ image: ←─────┼─ ImageInput.image
│ prompt: ←────┼─ TextInput.text
└──────┬───────┘
       │ response
       ↓
    분석 결과
```

**노드 설정**:
1. **ImageInput**: 도면 이미지 업로드
2. **TextInput**:
   - text: "이 도면의 모든 치수를 추출해주세요"
3. **VL Model**:
   - image: ImageInput의 `image` 연결
   - prompt: TextInput의 `text` 연결

**결과**: VL 모델이 이미지와 질문을 함께 받아서 치수 정보를 추출

---

### 예시 2: 다중 질문 처리

**워크플로우**:
```
                ┌─→ [VL Model 1] ─→ 심볼 검출 결과
                │    prompt: "용접 기호 찾기"
[ImageInput] ───┤
                │
                └─→ [VL Model 2] ─→ 치수 추출 결과
                     prompt: "치수 정보 추출"
```

**노드 구성**:
- ImageInput 1개
- TextInput 2개 (각각 다른 질문)
- VL Model 2개

---

## ⚙️ 현재 상태 및 제약사항

### 현재 프로젝트에서의 위치

**현재 도면 분석 파이프라인**:
```
Drawing Image → YOLO Detection → OCR Extraction → Tolerance Analysis → Quote PDF
```

- 모든 기존 API는 **이미지 입력만** 받음 (`requires_image: true`)
- TextInput은 **VL API의 프롬프트 입력용**으로만 제한적으로 사용 가능
- 대부분의 워크플로우는 **ImageInput만으로 충분**

### 제약사항

1. **비-이미지 API 없음**:
   - 현재 프로젝트에는 순수 텍스트 입력 API가 없음
   - Text-to-Image, GPT 같은 API 미구현

2. **VL API 제한적 지원**:
   - VL API가 텍스트 프롬프트를 받는지 확인 필요
   - 현재 VL API 구현 상태 검증 필요

3. **inputMappings 필요**:
   - Custom API가 텍스트를 받으려면 `inputMappings` 설정 필수
   - 예: `{"prompt": "inputs.text"}`

---

## 🚀 향후 확장 가능성

### Phase 1: VL API 프롬프트 기능 강화 (단기)

**목표**: VL 모델에 이미지 + 질문 동시 전달

**구현 필요 사항**:
1. VL API 서버에 `prompt` 파라미터 추가
2. VL API `/api/v1/info`에 inputs 정의:
   ```json
   {
     "inputs": [
       {"name": "image", "type": "file", "required": true},
       {"name": "prompt", "type": "string", "required": false}
     ]
   }
   ```
3. VL executor에서 prompt 처리 로직 추가

### Phase 2: Text-to-Image API 추가 (중기)

**목표**: 텍스트로 도면 스케치 생성

**사용 시나리오**:
- "선박 중간축 단면도 생성"
- "M12 볼트 도면 생성"

**구현**:
- Stable Diffusion, DALL-E 같은 모델 통합
- TextInput → Text-to-Image → ImageInput (재사용)

### Phase 3: LLM 기반 분석 체인 (장기)

**목표**: 도면 이미지 → OCR → LLM 분석 파이프라인

```
[ImageInput] → [OCR] → [TextInput: "공차 분석"] → [GPT-4] → 분석 보고서
```

**장점**:
- 자연어로 분석 요청
- 복잡한 추론 가능
- 커스터마이징 용이

---

## 📝 개발자 노트

### 백엔드 구현

**파일**: `gateway-api/blueprintflow/executors/textinput_executor.py`

```python
class TextInputExecutor(BaseNodeExecutor):
    async def execute(self, inputs: Dict[str, Any], context: Dict[str, Any]) -> Dict[str, Any]:
        text_value = self.parameters.get("text", "")

        if not text_value:
            raise ValueError("텍스트 입력이 비어있습니다.")

        return {
            "text": text_value,
            "length": len(text_value),
            "message": f"Text input provided: {len(text_value)} characters",
        }
```

**특징**:
- 파라미터에서 `text` 추출
- 최대 10,000자 제한
- 빈 문자열 검증

### 프론트엔드 구현

**파일**: `web-ui/src/config/nodeDefinitions.ts`

```typescript
textinput: {
  type: 'textinput',
  label: 'Text Input',
  category: 'input',
  color: '#8b5cf6',
  icon: 'Type',
  outputs: [
    { name: 'text', type: 'string', description: '📝 사용자가 입력한 텍스트' },
    { name: 'length', type: 'number', description: '📏 텍스트 길이' },
  ],
  parameters: [
    { name: 'text', type: 'string', default: '', description: '입력할 텍스트' },
  ],
}
```

---

## ❓ FAQ

### Q1: TextInput 없이도 텍스트를 입력할 수 없나요?
**A**: 현재는 불가능합니다. 노드 파라미터에는 텍스트를 입력할 수 있지만, 다른 노드의 **입력(input)**으로 전달하려면 TextInput 노드가 필요합니다.

### Q2: 현재 프로젝트에서 꼭 필요한가요?
**A**: 현재는 선택사항입니다. 모든 기존 API는 이미지만 받습니다. 하지만 **VL API의 프롬프트 기능**이나 **향후 LLM 통합**을 위해 보관합니다.

### Q3: VL API에 어떻게 사용하나요?
**A**: VL API가 `prompt` 입력을 지원하도록 업데이트된 후, ImageInput과 TextInput을 동시에 연결하면 됩니다.

### Q4: 다른 Input 노드도 추가될 예정인가요?
**A**: 향후 필요에 따라 추가 가능합니다:
- **NumberInput**: 숫자 입력 (시뮬레이션 파라미터)
- **FileInput**: 일반 파일 업로드 (PDF, CSV)
- **JSONInput**: 구조화된 데이터 입력

---

## 🔗 관련 문서

- [BlueprintFlow 개요](01_overview.md)
- [노드 타입 설명](02_node_types.md)
- [VL API 문서](../api/vl/README.md)
- [비-이미지 API 구현 가이드](/tmp/non_image_api_implementation_guide.md)

---

**Last Updated**: 2025-11-22
**Status**: ✅ 구현 완료 (향후 확장용)
**Version**: 1.0
