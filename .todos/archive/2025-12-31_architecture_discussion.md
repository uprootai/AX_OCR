# 아키텍처 논의: Builder vs AI BOM 세션 설정

> **작성일**: 2025-12-31
> **최종 업데이트**: 2025-12-31
> **상태**: P1 완료, 진행 중
> **관련**: BlueprintFlow Builder, Blueprint AI BOM WorkflowPage

---

## 1. 현재 상태 분석

### 1.1 기능 중복 현황

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  BlueprintFlow Builder (web-ui)          Blueprint AI BOM (세션 UI)     │
│  ─────────────────────────────           ──────────────────────────     │
│                                                                         │
│  ┌──────────────────────┐                ┌──────────────────────┐      │
│  │ ImageInput 노드       │                │ 사이드바              │      │
│  │ ┌──────────────────┐ │                │ ┌──────────────────┐ │      │
│  │ │ features 선택    │ │    ????        │ │ 분석 옵션        │ │      │
│  │ │ □ symbol_detection│ │ ◄──────────►  │ │ □ 심볼 검출      │ │      │
│  │ │ □ dimension_ocr  │ │   동기화?      │ │ □ 치수 OCR       │ │      │
│  │ │ □ pid_connectivity│ │               │ │ □ P&ID 연결성    │ │      │
│  │ │ □ gdt_parsing    │ │                │ │ □ GD&T 파싱      │ │      │
│  │ └──────────────────┘ │                │ └──────────────────┘ │      │
│  └──────────────────────┘                └──────────────────────┘      │
│                                                                         │
│  ┌──────────────────────┐                ┌──────────────────────┐      │
│  │ YOLO 노드            │                │ 메인 콘텐츠           │      │
│  │ ┌──────────────────┐ │                │ ┌──────────────────┐ │      │
│  │ │ model_type 선택  │ │                │ │ 활성화된 기능     │ │      │
│  │ │ ○ engineering   │ │                │ │ (features 기반)  │ │      │
│  │ │ ○ pid_class_aware│ │                │ └──────────────────┘ │      │
│  │ └──────────────────┘ │                └──────────────────────┘      │
│  └──────────────────────┘                                              │
│                                                                         │
│  현재 흐름:                                                             │
│  Builder features → /sessions/upload?features=... → 세션 생성          │
│                                                     ↓                   │
│                                          세션에 features 저장           │
│                                                     ↓                   │
│                                          AI BOM UI에서 표시             │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 1.2 문제점

| 문제 | 현상 | 영향 |
|------|------|------|
| **중복 설정** | Builder에서 선택한 features가 AI BOM 사이드바에도 표시 | 사용자 혼란 |
| **동기화 불명확** | 사이드바에서 변경하면 Builder 설정과 불일치 | 예측 불가능한 동작 |
| **책임 분리 불명확** | 어디서 features를 제어해야 하는지 모호 | 유지보수 어려움 |

### 1.3 현재 코드 분석

**Builder에서 features 전달**:
```typescript
// web-ui에서 API 호출 시
const formData = new FormData();
formData.append('file', imageFile);
formData.append('features', enabledFeatures.join(','));
// → POST /sessions/upload
```

**AI BOM 세션에서 features 사용**:
```typescript
// WorkflowPage.tsx
const visibility = getSectionVisibility(drawingType, session.features);
// → features 기반으로 섹션 가시성 결정
```

**사이드바 "분석 옵션"**:
```typescript
// 현재: 읽기 전용으로 표시만 (변경 불가)
// 또는: drawing_type 변경 시 기본 features로 덮어쓰기
```

---

## 2. 논의 포인트

### 2.1 Builder features vs AI BOM 사이드바

**질문**: Builder에서 features를 이미 선택했는데, AI BOM 세션 사이드바에서 다시 설정하는 것이 맞는가?

**현재 동작**:
```
┌────────────────────────────────────────────────────────────────────────┐
│  시나리오 1: Builder에서 features 선택 후 실행                          │
│                                                                        │
│  1. Builder에서 [symbol_detection, pid_connectivity] 선택              │
│  2. BlueprintAIBOM 노드 실행                                           │
│  3. AI BOM 세션 생성 (features = [symbol_detection, pid_connectivity]) │
│  4. AI BOM UI 열림 → 활성화된 기능 표시                                 │
│                                                                        │
│  결과: Builder 설정이 그대로 반영됨 ✅                                   │
├────────────────────────────────────────────────────────────────────────┤
│  시나리오 2: AI BOM 직접 접속 (Builder 우회)                            │
│                                                                        │
│  1. http://localhost:5021 직접 접속                                    │
│  2. 이미지 업로드 (features 미지정)                                    │
│  3. 세션 생성 (features = [])                                          │
│  4. 사용자가 사이드바에서 분석 옵션 선택 필요                           │
│                                                                        │
│  결과: 사이드바 설정 필요 ⚠️                                            │
├────────────────────────────────────────────────────────────────────────┤
│  시나리오 3: Builder 실행 후 AI BOM에서 features 변경                   │
│                                                                        │
│  1. Builder에서 [symbol_detection] 선택                                │
│  2. AI BOM 세션 생성                                                   │
│  3. AI BOM 사이드바에서 [pid_connectivity] 추가 활성화                  │
│  4. ??? → Builder 설정과 불일치                                        │
│                                                                        │
│  결과: 동기화 문제 발생 ❌                                               │
└────────────────────────────────────────────────────────────────────────┘
```

**옵션 비교**:

| 옵션 | 설명 | 장점 | 단점 |
|------|------|------|------|
| **A: Builder 우선** | 사이드바는 읽기 전용, Builder에서만 features 변경 | 일관성 보장 | 유연성 감소 |
| **B: AI BOM 우선** | 사이드바에서 자유롭게 변경, Builder 설정은 초기값만 | 유연성 | 동기화 문제 |
| **C: 하이브리드** | Builder = 기본값, AI BOM = 세션별 오버라이드 허용 | 균형 | 복잡도 증가 |
| **D: 통합** | 사이드바 제거, Builder 설정만 사용 | 단순함 | AI BOM 독립 사용 불가 |

**권장**: **옵션 C (하이브리드)**
- Builder에서 설정한 features는 세션 생성 시 기본값
- AI BOM 세션에서 필요 시 오버라이드 가능
- 오버라이드 시 "Builder 설정과 다름" 경고 표시

### 2.2 두 기능의 일치 여부

**현재 상태**: ✅ 완벽히 일치 (2025-12-31 P1 완료)

```
┌─────────────────────────────────────────────────────────────────────────┐
│  Builder (featureDefinitions.ts)          AI BOM (sectionConfig.ts)    │
│  ──────────────────────────────           ─────────────────────────    │
│                                                                         │
│  25개 features 정의                       22개 섹션 가시성 정의          │
│                                                                         │
│  symbol_detection        ◄──────────────► symbolDetection        ✅    │
│  dimension_ocr           ◄──────────────► dimensionOCR           ✅    │
│  line_detection          ◄──────────────► lineDetection          ✅    │
│  gdt_parsing             ◄──────────────► gdtParsing             ✅    │
│  pid_connectivity        ◄──────────────► pidConnectivity        ✅    │
│  pid_valve_detection     ◄──────────────► valveSignalList        ✅    │
│  pid_equipment_detection ◄──────────────► equipmentList          ✅    │
│  pid_design_checklist    ◄──────────────► bwmsChecklist          ✅    │
│  pid_deviation_analysis  ◄──────────────► deviationList          ✅    │
│                                                                         │
│  gt_comparison           ◄──────────────► gtComparison           ✅    │
│  bom_generation          ◄──────────────► bomGeneration          ✅    │
│  industry_equipment_detection ◄─────────► industryEquipmentDetection ✅│
│                                                                         │
│  결론: 모든 기능 매핑 완료 ✅                                            │
└─────────────────────────────────────────────────────────────────────────┘
```

**완료 사항** (2025-12-31):
1. ✅ `sectionConfig.ts`에 누락된 섹션 추가 (gt_comparison, bom_generation, industry_equipment_detection)
2. ✅ `SectionVisibility` 타입 확장
3. ✅ `ALL_AVAILABLE_FEATURES` 목록 업데이트

### 2.3 참조 이미지 패널 개선

**현재 상태**:
```
┌─────────────────────────────────────────────────────────────────────────┐
│  AI BOM WorkflowPage                                                    │
│  ┌─────────────┬────────────────────────────┬──────────────────────┐   │
│  │ 사이드바     │        메인 콘텐츠          │   참조 이미지 패널   │   │
│  │ (세션 목록) │  (도면 + 분석 결과)         │   (27개 클래스)     │   │
│  │             │                            │   ┌──────────────┐  │   │
│  │             │                            │   │ PT           │  │   │
│  │             │                            │   │ 정류기        │  │   │
│  │             │                            │   │ GIS          │  │   │
│  │             │                            │   │ ARRESTER     │  │   │
│  │             │                            │   │ ...          │  │   │
│  │             │                            │   └──────────────┘  │   │
│  └─────────────┴────────────────────────────┴──────────────────────┘   │
│                                                                         │
│  문제점:                                                                │
│  1. 27개 클래스가 하드코딩됨 (전력 설비 기준)                           │
│  2. P&ID 도면에서는 다른 참조 이미지 필요                               │
│  3. 사용자가 커스텀 참조 이미지 추가 불가                               │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**제안: Builder에 ReferenceImage 노드 추가**

```
┌─────────────────────────────────────────────────────────────────────────┐
│  BlueprintFlow Builder                                                  │
│                                                                         │
│  ┌──────────────┐     ┌──────────────┐     ┌──────────────┐           │
│  │ ImageInput   │────►│    YOLO      │────►│BlueprintAIBOM│           │
│  │ (도면)       │     │              │     │              │           │
│  └──────────────┘     └──────────────┘     └──────┬───────┘           │
│                                                   │                    │
│  ┌──────────────┐                                 │                    │
│  │ReferenceImage│─────────────────────────────────┘                    │
│  │ (참조 이미지)│  ← 새로운 노드                                       │
│  │              │                                                      │
│  │ ┌──────────┐│                                                       │
│  │ │ 업로드   ││ ← 사용자가 참조 이미지 폴더 업로드                    │
│  │ │ 또는     ││                                                       │
│  │ │ 프리셋   ││ ← 사전 정의된 프리셋 (전력설비, P&ID, 기계부품)       │
│  │ └──────────┘│                                                       │
│  └──────────────┘                                                      │
│                                                                         │
│  동작:                                                                  │
│  - ReferenceImage 노드 연결됨: 해당 참조 이미지 사용                    │
│  - ReferenceImage 노드 없음: 기본 참조 이미지 또는 패널 숨김            │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**장점**:
1. 도면 타입별로 적절한 참조 이미지 세트 선택 가능
2. 사용자 커스텀 참조 이미지 지원
3. 불필요한 경우 참조 패널 숨기기 가능
4. Builder에서 워크플로우 전체를 제어 (일관성)

**구현 난이도**: 중간 (1-2주)
- [ ] ReferenceImage 노드 타입 정의
- [ ] 참조 이미지 프리셋 관리 API
- [ ] AI BOM에서 세션 메타데이터로 참조 이미지 경로 전달
- [ ] WorkflowPage에서 동적 참조 패널 렌더링

---

## 3. 권장 아키텍처

### 3.1 목표

```
┌─────────────────────────────────────────────────────────────────────────┐
│  원칙: Builder = Single Source of Truth                                 │
│                                                                         │
│  - 모든 설정은 Builder에서 정의                                         │
│  - AI BOM은 Builder 설정을 실행/표시하는 뷰어 역할                      │
│  - AI BOM에서의 변경은 "세션 로컬 오버라이드"로 처리                    │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 3.2 개선된 데이터 흐름

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  BlueprintFlow Builder                                                  │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                                                                   │  │
│  │  ImageInput ──► YOLO ──► OCR ──► BlueprintAIBOM                  │  │
│  │      │                              │                             │  │
│  │      │ features                     │ 세션 생성                   │  │
│  │      ▼                              ▼                             │  │
│  │  [symbol_detection,              세션 메타데이터:                 │  │
│  │   pid_connectivity,              {                                │  │
│  │   pid_valve_detection]             features: [...],              │  │
│  │                                    reference_images: "pid",       │  │
│  │  ReferenceImage ─────────────────► drawing_type: "pid",          │  │
│  │      │                             source: "builder"              │  │
│  │      │ preset="pid"              }                                │  │
│  │      ▼                                                            │  │
│  │  BWMS P&ID 참조 이미지                                            │  │
│  │                                                                   │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                     │                                   │
│                                     ▼                                   │
│  Blueprint AI BOM                                                       │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                                                                   │  │
│  │  사이드바              메인 콘텐츠              참조 패널         │  │
│  │  ┌──────────┐         ┌──────────────┐        ┌──────────┐      │  │
│  │  │활성 기능 │         │ 세션 내용    │        │P&ID 참조 │      │  │
│  │  │(읽기전용)│         │              │        │이미지    │      │  │
│  │  │          │         │              │        │          │      │  │
│  │  │⚠️ Builder │         │              │        │(동적로드)│      │  │
│  │  │설정 기준 │         │              │        │          │      │  │
│  │  └──────────┘         └──────────────┘        └──────────┘      │  │
│  │                                                                   │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 3.3 구현 우선순위

| 우선순위 | 작업 | 예상 공수 | 영향도 | 상태 |
|:--------:|------|----------|--------|:----:|
| P1 | 사이드바 "분석 옵션"에 "Builder 설정" 라벨 표시 | 0.5일 | 중 | ✅ 완료 |
| P1 | feature 매핑 완성 (누락된 섹션 추가) | 1일 | 중 | ✅ 완료 |
| P2 | ReferenceImage 노드 설계 | 2일 | 고 | 📋 |
| P2 | 참조 이미지 프리셋 API | 3일 | 고 | 📋 |
| P3 | AI BOM 세션 오버라이드 + 경고 UI | 2일 | 중 | 📋 |

---

## 4. 결론

### 4.1 현재 상태 평가

| 항목 | 상태 | 비고 |
|------|:----:|------|
| Builder → AI BOM features 전달 | ✅ | 정상 동작 |
| features ↔ 섹션 매핑 | ⚠️ | 일부 누락 |
| 사이드바 분석 옵션 역할 | ❓ | 명확화 필요 |
| 참조 이미지 패널 | ⚠️ | 하드코딩, 유연성 부족 |

### 4.2 권장 방향

1. **단기**: 사이드바에 "Builder 설정 기준" 라벨 추가, 읽기 전용 명시
2. **중기**: ReferenceImage 노드 추가, 참조 이미지 동적 로드
3. **장기**: Builder를 완전한 SSOT로, AI BOM은 뷰어 역할로 명확화

---

**작성자**: Claude Code (Opus 4.5)
**다음 논의**: TECHCROSS 팀 피드백 수집 후
