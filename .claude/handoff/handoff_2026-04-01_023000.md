# 세션 핸드오프 - 2026-04-01 02:30:00

## 1. 세션 요약
docs-site(Starlight) 리빌드/배포, DSE Bearing 실험 문서 전체 점검 및 하드코딩 제거, S05 CircleNet 합성 데이터 학습(v1~v3), S07 Codex GPT-5.4 vision 테스트(T1/T5 3/3 성공), S08 Cardinal Projection 신규 실험 설계 및 프로토타입 구현.

## 2. 작업 중인 태스크
- [x] docs-site Starlight 빌드 경로 수정 (baseUrl 문제 해결)
- [x] DSE 28개 문서 전체 점검 — 이미지 참조 검증
- [x] 하드코딩 제거 — dimension_ensemble.py, generate_experiment_captures_v2.py, generate_e12_captures.py
- [x] S01 이미지 재생성 (자동 감지 기반 녹색 박스)
- [x] pipeline/index.mdx — K/L/M/N 전부 디버그 오버레이 이미지 추가
- [x] S05 CircleNet — 합성 데이터 학습 (v1 256px → v2 512px → v3 실제배경)
- [x] S07 Codex GPT-5.4 vision — T1 3/3, T5 3/3 성공
- [ ] **S08 Cardinal Projection — 단계별 캡쳐 생성 (미완료)**

## 3. 수정된 파일
| 파일 | 변경 유형 | 설명 |
|------|----------|------|
| `dimension_ensemble.py` | Modified | SEC 하드코딩 폴백 제거 → 자동감지 실패 시 스킵 |
| `generate_experiment_captures_v2.py` | Modified | `SEC=` 하드코딩 삭제 → `_auto_detect_section()` 사용 |
| `generate_e12_captures.py` | Modified | 하드코딩 `0.53/0.78` 삭제 → `_auto_detect_section()` 사용 |
| `scripts/s05_synthetic_train.py` | Created | 합성 원 데이터 생성 + TinyUNet 학습 |
| `scripts/s08_cardinal_projection.py` | Created | Cardinal Projection 프로토타입 |
| `experiments/s05-circlenet.mdx` | Modified | v1~v3 실험 결과 + 실패 분석 |
| `experiments/s08-cardinal.mdx` | Created | S08 알고리즘 설계 + 프로토타입 결과 |
| `pipeline/index.mdx` | Modified | L/M/N 디버그 오버레이 이미지 추가 |
| `ensemble/voting-problem.mdx` | Modified | 깨진 이미지 참조 수정 |
| `e12-section-detection.mdx` | Modified | 하드코딩 제거 반영 |
| S01~S04 실험 이미지 28장 | Regenerated | 자동감지 기반 녹색 박스 |

## 4. 미완료 작업
- [x] S08 단계별 캡쳐 6장 생성 완료
- [x] Ring Scan 대안 시도 → 내원 엣지 자체가 없음 확인
- [x] Combo4 (K+S01+L) 시도 → 화살촉이 원 바깥 참조선에 위치하여 실패
- [x] 최종 결론 문서화: ASSY에서 원 검출 기반 접근은 구조적 한계
- **커밋 미완료**: 이번 세션 변경사항 전부 uncommitted

## 5. 중요 결정사항
- **하드코딩 전면 금지**: SEC 크롭 비율, 캡쳐 스크립트 모두 `_auto_detect_section()` 사용. 실패 시 스킵 (폴백 없음)
- **S05 CircleNet**: 합성 데이터로 반지름 회귀 성공 입증. 하지만 실제 도면에서 도메인 갭으로 실패. 실용적 가치 낮음 → 기록만 유지
- **S07 Codex vision**: GPT-5.4가 T1/T5 모두 3/3 성공. 현재 가장 효과적인 OD/ID/W 추출 방법
- **S08 근본 한계**: ASSY 도면에서 내원(ID)이 부품/해칭으로 가려져 원 검출 불가. 외원(OD)만 Contour로 부분 성공

## 6. 다음 세션에서 할 일
1. **S08 단계별 캡쳐 생성** — 원검출/투사/화살촉매칭/Ø|R판별/OCR/최종 6단계 이미지
2. **S08 원 검출 개선** — RANSAC + Contour 조합, 정면도 영역 한정
3. **S08 docs 업데이트** — 단계별 캡쳐 반영
4. **커밋** — 이번 세션 전체 변경사항

## 7. Curated Context
- **Task state**: S08 Cardinal Projection 프로토타입 완료, 단계별 캡쳐 미생성
- **Relevant files**:
  - `scripts/s08_cardinal_projection.py` — S08 프로토타입
  - `experiments/s08-cardinal.mdx` — S08 docs 페이지
  - `services/dimension_ensemble.py` — `_auto_detect_section()` 함수
  - `services/arrowhead_detector.py` — `detect_arrowheads()`, `match_arrowhead_pairs()`
- **Confirmed facts**:
  - T1 정면도 Contour 원 검출: r=205 (GT OD r=180, 근접). 내원(GT ID r=95) 미검출
  - RANSAC: T1 r=249 (support 2.05), T5 r=476~533 (1.37~1.95)
  - HoughCircles: 도면에서 프레임(r=778)만 검출 — 사실상 폐기
  - Codex GPT-5.4 vision: T1 3/3, T5 3/3 완벽 성공
- **Open risks**: ASSY 도면 내원 검출 근본 한계 (부품/해칭 overlap)
- **Next action**: Thrust 도면에서 S08 Cardinal Projection 시도 (K 원 검출 성공 확인). `geometry_guided_extractor._detect_circles`를 S08에 통합하여 Thrust에서 화살촉 매칭 테스트.
- **핵심 발견**: K의 `_detect_circles`는 Thrust에서 외원(664)+내원(547) 모두 검출 성공. T1은 내원 미검출(구조적). ASSY 도면에서 화살촉은 원 위가 아닌 참조선에 위치 — Cardinal 직접 매칭 불가.
- **Codex GPT-5.4**: T1/T5 3/3 완벽. 현재 최선의 방법.

## 8. 참고 컨텍스트
- docs-site: http://localhost:3001/customer-cases/dsebearing/experiments/s08-cardinal/
- S08 결과 JSON: `docs-site-starlight/public/images/experiments/s08_results.json`
- S05 모델: `scripts/s05_data/models/circlenet_v3.pt`
- S07 결과: `/tmp/s07_t1_result.json`, `/tmp/s07_t5_result.json`
- PaddleOCR: http://localhost:5006 (정상)
- docs-site container: `docs-site` (:3001)

---
**생성 시간**: 2026-04-01T02:30:00
**컨텍스트 사용률**: ~80%
