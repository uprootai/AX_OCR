---
sidebar_position: 4
title: 포트 및 네트워크
description: 포트 할당 및 네트워크 토폴로지
---

import PortMap from '@site/src/components/PortMap';
import ArchitectureDiagram from '@site/src/components/ArchitectureDiagram';

# 포트 및 네트워크 맵

## 포트 할당

<PortMap />

## Docker 네트워크

모든 서비스는 `ax_poc_network` (bridge) 네트워크를 공유합니다.

<ArchitectureDiagram />

## 서비스 의존성

```mermaid
flowchart TD
    GW["Gateway API"] --> YOLO & EDOCR & VL
    GW --> PADDLE & TESS & TROCR
    GW --> ENS & SURYA & DOCTR & EASY
    GW --> EDGE & LINE & ESRGAN
    GW --> SKIN & PID & DC & COMP
    GW --> BOM & KN

    KN["Knowledge"] --> NEO["Neo4j"]
    ENS["OCR Ensemble"] --> EDOCR & PADDLE & TESS & TROCR

    BOM["BOM 백엔드"] --> YOLO
    BOM --> EDOCR
    BOM --> VL["Vision-Language"]
```

## 헬스체크 엔드포인트(Health Check Endpoints)

모든 서비스는 표준 헬스체크를 제공합니다:

```bash
# Gateway
GET http://localhost:8000/api/v1/health

# 개별 서비스
GET http://localhost:<port>/health

# BOM 백엔드
GET http://localhost:5020/health
```

| 서비스 | 헬스 엔드포인트 | 예상 응답 |
|--------|----------------|-----------|
| Gateway | `/api/v1/health` | `{"status": "healthy"}` |
| 전체 ML API | `/health` | `{"status": "healthy"}` |
| Neo4j | `:7474` (HTTP) | 브라우저 UI |
