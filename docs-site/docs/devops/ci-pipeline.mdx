---
sidebar_position: 2
title: CI 파이프라인
description: 지속적 통합 파이프라인
---

# CI 파이프라인

> Git Push/PR 시 프론트엔드(ESLint, Build, Vitest)와 백엔드(Ruff, Pytest)를 병렬 검증하는 지속적 통합 파이프라인.

## 워크플로우

<FlowDiagram
  direction="TD"
  nodes={[
    { id: 'push', label: 'Git Push / PR', color: '#37474f', bg: '#eceff1', shape: 'rounded' },
    { id: 'ci', label: 'CI 워크플로우', color: '#1565c0', bg: '#e3f2fd', shape: 'rounded' },
    { id: 'fe', label: '프론트엔드 작업', color: '#1565c0', bg: '#e3f2fd', shape: 'rounded' },
    { id: 'be', label: '백엔드 작업', color: '#4a148c', bg: '#f3e5f5', shape: 'rounded' },
    { id: 'lint', label: 'ESLint', sub: '에러 0건', color: '#1565c0', bg: '#e3f2fd', shape: 'rect' },
    { id: 'build', label: 'npm run build', sub: '프로덕션 빌드', color: '#1565c0', bg: '#e3f2fd', shape: 'rect' },
    { id: 'testfe', label: 'Vitest', sub: '185개 테스트', color: '#1565c0', bg: '#e3f2fd', shape: 'rect' },
    { id: 'ruff', label: 'Ruff 린터', sub: 'Python 린트', color: '#4a148c', bg: '#f3e5f5', shape: 'rect' },
    { id: 'pytest', label: 'Pytest', sub: '364개 테스트', color: '#4a148c', bg: '#f3e5f5', shape: 'rect' },
    { id: 'sum', label: '요약', color: '#37474f', bg: '#eceff1', shape: 'rounded' },
    { id: 'ready', label: '머지 준비 완료', color: '#2e7d32', bg: '#e8f5e9', shape: 'stadium' },
    { id: 'block', label: 'PR 차단', color: '#c62828', bg: '#ffebee', shape: 'stadium' },
  ]}
  edges={[
    { from: 'push', to: 'ci' },
    { from: 'ci', to: 'fe' },
    { from: 'ci', to: 'be' },
    { from: 'fe', to: 'lint' },
    { from: 'fe', to: 'build' },
    { from: 'fe', to: 'testfe' },
    { from: 'be', to: 'ruff' },
    { from: 'be', to: 'pytest' },
    { from: 'lint', to: 'sum' },
    { from: 'build', to: 'sum' },
    { from: 'testfe', to: 'sum' },
    { from: 'ruff', to: 'sum' },
    { from: 'pytest', to: 'sum' },
    { from: 'sum', to: 'ready', label: '모두 통과' },
    { from: 'sum', to: 'block', label: '실패' },
  ]}
  layers={[['push'], ['ci'], ['fe', 'be'], ['lint', 'build', 'testfe', 'ruff', 'pytest'], ['sum'], ['ready', 'block']]}
/>

## 트리거

```yaml
on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
```

## 작업(Jobs)

### 프론트엔드

| 단계 | 명령어 | 소요 시간 |
|------|--------|----------|
| 설치 | `npm ci` | ~30초 |
| 린트 | `npm run lint` | ~10초 |
| 빌드 | `npm run build` | ~30초 |
| 테스트 | `npm run test:run` | ~15초 |

### 백엔드

| 단계 | 명령어 | 소요 시간 |
|------|--------|----------|
| 설치 | `pip install -r requirements.txt` | ~30초 |
| 린트 | `ruff check .` | ~5초 |
| 테스트 | `pytest tests/ -v` | ~60초 |

## 테스트 요약

| 테스트 스위트 | 테스트 수 | 프레임워크 |
|--------------|----------|-----------|
| 프론트엔드 (web-ui) | 185 | Vitest |
| 백엔드 (gateway-api) | 364 | Pytest |
| **합계** | **549** | - |

## 품질 게이트(Quality Gates)

| 게이트 | 기준 | 상태 |
|--------|------|------|
| TypeScript 빌드 | 에러 0건 | 필수 |
| ESLint | 에러 0건 | 필수 |
| 프론트엔드 테스트 | 전체 통과 | 필수 |
| Ruff | 에러 0건 | 필수 |
| 백엔드 테스트 | 전체 통과 | 필수 |

## 관련 문서

- [CD 파이프라인](/docs/devops/cd-pipeline) — 지속적 배포 파이프라인
- [Docker Compose](/docs/devops/docker-compose) — 컨테이너 오케스트레이션
- [DevOps 및 인프라](/docs/devops) — DevOps 개요
