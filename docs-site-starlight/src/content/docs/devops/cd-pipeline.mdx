---
sidebar_position: 3
title: CD 파이프라인
description: 지속적 배포 파이프라인
tags: [DevOps, Docker, CD, 배포]
---

import FlowDiagram from '../../../components/diagrams/FlowDiagram';

# CD 파이프라인 (CD Pipeline)

> CI 성공 후 스테이징 배포, 스모크 테스트, 수동 승인을 거쳐 프로덕션에 배포하는 지속적 배포 파이프라인.

## 워크플로우

<FlowDiagram client:load
  direction="TD"
  nodes={[
    { id: 'trigger', label: 'CI 성공 / 수동 실행', color: '#37474f', bg: '#eceff1', shape: 'rounded' },
    { id: 'pre', label: '사전 점검', sub: '헬스 + 디스크', color: '#1565c0', bg: '#e3f2fd', shape: 'rounded' },
    { id: 'build', label: '이미지 빌드', sub: 'Docker 이미지 6개', color: '#4a148c', bg: '#f3e5f5', shape: 'rounded' },
    { id: 'stage', label: '스테이징 배포', color: '#e65100', bg: '#fff3e0', shape: 'rounded' },
    { id: 'smoke', label: '스모크 테스트', sub: '헬스 체크', color: '#00838f', bg: '#e0f7fa', shape: 'rounded' },
    { id: 'gate', label: '수동 승인', sub: '프로덕션 게이트', color: '#311b92', bg: '#ede7f6', shape: 'diamond' },
    { id: 'prod', label: '프로덕션 배포', color: '#c62828', bg: '#ffebee', shape: 'rounded' },
    { id: 'verify', label: '헬스 검증', color: '#00838f', bg: '#e0f7fa', shape: 'rounded' },
    { id: 'done', label: '배포 완료', color: '#2e7d32', bg: '#e8f5e9', shape: 'stadium' },
    { id: 'rollback', label: '롤백', color: '#c62828', bg: '#ffebee', shape: 'stadium' },
  ]}
  edges={[
    { from: 'trigger', to: 'pre' },
    { from: 'pre', to: 'build' },
    { from: 'build', to: 'stage' },
    { from: 'stage', to: 'smoke' },
    { from: 'smoke', to: 'gate', label: '통과' },
    { from: 'smoke', to: 'rollback', label: '실패', dashed: true },
    { from: 'gate', to: 'prod' },
    { from: 'prod', to: 'verify' },
    { from: 'verify', to: 'done', label: '통과' },
    { from: 'verify', to: 'rollback', label: '실패', dashed: true },
  ]}
  layers={[['trigger'], ['pre'], ['build'], ['stage'], ['smoke'], ['gate', 'rollback'], ['prod'], ['verify'], ['done']]}
/>

## 트리거

```yaml
on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]
    branches: [main]
  workflow_dispatch:  # 수동 트리거
```

## 배포 단계

### 1. 사전 점검(Pre-check)

- 현재 서비스 건강 상태 확인
- 디스크 공간 확인
- Docker 데몬 상태 확인

### 2. 이미지 빌드(Build Images)

6개 핵심 이미지 빌드:

| 이미지 | 소스 경로 |
|--------|----------|
| `gateway-api` | `gateway-api/` |
| `web-ui` | `web-ui/` |
| `blueprint-ai-bom-backend` | `blueprint-ai-bom/backend/` |
| `blueprint-ai-bom-frontend` | `blueprint-ai-bom/frontend/` |
| `yolo-api` | `models/yolo-api/` |
| `edocr2-v2-api` | `models/edocr2-v2-api/` |

### 3. 스테이징 배포(Staging Deploy)

- 스테이징 환경에 새 이미지 배포
- 5분 안정화 대기

### 4. 스모크 테스트(Smoke Tests)

```bash
# 모든 서비스 헬스 체크
curl -s http://staging:8000/api/v1/health
curl -s http://staging:5020/health
# ... 전체 포트
```

### 5. 프로덕션 배포 (수동 승인 필요)

- GitHub Environment 보호 규칙 적용
- 승인 후 프로덕션 배포 시작

### 6. 롤백(Rollback)

실패 시 이전 버전으로 자동 롤백:

```bash
docker-compose pull  # 이전 태그 이미지
docker-compose up -d
```

## 환경 변수

| 변수 | 설명 |
|------|------|
| `DEPLOY_ENV` | 스테이징 / 프로덕션 |
| `IMAGE_TAG` | Docker 이미지 태그 (git SHA) |
| `ROLLBACK_TAG` | 이전 안정 태그 |

## 관련 API

| 메서드 | 엔드포인트 | 설명 |
|--------|-----------|------|
| POST | `Gateway /admin/docker/restart/:service` | 서비스 재시작 |

## 관련 문서

- [CI 파이프라인](/devops/ci-pipeline) — 지속적 통합 파이프라인
- [Docker Compose](/devops/docker-compose) — 컨테이너 오케스트레이션
- [DevOps 및 인프라](/devops) — DevOps 개요
