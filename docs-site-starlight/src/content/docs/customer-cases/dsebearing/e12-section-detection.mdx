---
sidebar_position: 21
title: E12 — SECTION 단면도 감지 실험
description: ASSY 도면에서 SECTION E-3 영역을 크롭하여 OCR → 외경/내경/폭 추출. 값 발견 성공, 분류 로직 개선 필요.
tags: [dimension-lab, 앙상블, SECTION, 크롭, E12]
---

# E12 — SECTION 단면도 감지 실험

> **목적**: K geometry가 정면도 케이싱 원을 잡는 문제를 SECTION 단면도 영역 직접 OCR로 우회
> **실험일**: 2026-03-30
> **결론**: SECTION 영역에서 핵심 치수(외경/내경/폭) 발견 성공. 단순 크기 기반 분류는 실패 — 위치 기반 분류 필요.

---

## 접근법

ASSY 도면은 일관된 레이아웃을 가짐:
- **좌측**: 정면도 (원형 단면 — 케이싱 외곽)
- **우측**: SECTION E-3 단면도 (직사각형 — 베어링 내부 구조)
- **상단**: 측면도 (축 방향 치수)

K geometry는 정면도의 원을 잡아 케이싱 외경을 반환. 실제 베어링 외경/내경은 SECTION 단면도에만 존재.

**방법**: 이미지 우측 영역(x: 53%~78%, y: 25%~92%)을 크롭 → PaddleOCR → 숫자 추출 → 분류

---

## 실험 결과

| 도면 | GT(외/내/폭) | SECTION OCR 숫자 | 추출(외/내/폭) | 외경 | 내경 | 폭 |
|---|---|---|---|---|---|---|
| T1 | 360/190/200 | (250), (200), 200, **190** | 250/200/190 | ✗ | ✗ | ✗ |
| T2 | 380/190/200 | 250, (200), 200, **190** | 250/200/190 | ✗ | ✗ | ✗ |
| T3 | 380/260/? | (700), (310), **260**, (200) | 700/310/260 | ✗ | ✗ | ? |
| T4 | 420/260/? | **(0420)**, (310), **(260)**, (200) | **420**/310/260 | ✓ | ✗ | ? |
| T5 | 1036/580/200 | 735NM, 297, **260**, **200** | 735/297/260 | ✗ | ✗ | ✗ |
| T8 | 500/260/200 | (297), **(260)**, **(200)** | 297/**260**/**200** | ✗ | ✓ | ✓ |
| Thrust | 515/440/48 | 920, 460, (450), (380), **48.5** | 920/460/450 | ✗ | ✗ | ✗ |

**외경 1/7, 내경 1/7, 폭 1/7** (단순 크기 기반 분류)

---

## 핵심 발견

### 긍정적
1. **GT 값이 OCR에서 발견됨** — 190(T1/T2), 260(T3/T4/T5/T8), 200(T1/T2/T8), 420(T4), 48.5(Thrust)
2. **SECTION E-3 텍스트 자체도 OCR로 잡힘** — 영역 감지 자동화 가능
3. **크롭 영역이 일관적** — 7건 모두 같은 비율(53%~78%)로 SECTION 영역 커버

### 부정적
1. **단순 "가장 큰 값 = 외경" 분류 실패** — 케이싱 높이(700, 735) 등이 외경보다 큼
2. **Ø 기호가 잘 안 잡힘** — SECTION 단면도의 Ø380 같은 값이 `(0420)` 등으로 잡힘
3. **외경 치수가 세로 방향(우측)에 있음** — 위치 기반 분류 필요

---

## 분류 개선 방향

### SECTION 단면도의 치수 배치 패턴

```
         (폭)
    ┌────────────┐
    │            │ Ø외경 (우측, 세로 방향)
    │   (내경)   │
    │            │
    └────────────┘
         (하단 폭)
```

- **외경**: Ø 기호 + 우측 세로 방향 치수 (x좌표가 큰 영역)
- **내경**: 가운데 가로 방향 치수 (외경보다 작음)
- **폭**: 상단/하단 가로 방향 치수

### 구현 계획
1. 크롭 영역 내 텍스트의 **x/y 좌표** 활용
2. **우측 영역(x > 70%)의 세로 텍스트** → 외경 후보
3. **중앙 영역의 가로 텍스트** → 내경 후보
4. **상단/하단의 가로 텍스트** → 폭 후보
5. Ø 기호 우선, 괄호 치수 참고

---

## 코드

### 크롭 영역 정의
```python
# ASSY 도면 SECTION 영역 비율 (7건 모두 동일)
section_crop = img.crop((
    int(w * 0.53),   # x 시작: 53%
    int(h * 0.25),   # y 시작: 25%
    int(w * 0.78),   # x 끝: 78%
    int(h * 0.92),   # y 끝: 92%
))
```

### OCR 호출
```python
r = requests.post("http://localhost:5006/api/v1/ocr", files={"file": f})
detections = r.json()["detections"]
```

---

## 위치 기반 분류 결과 (개선)

단순 크기 기반 → **위치 기반 분류** 적용:

| 도면 | GT(외/내/폭) | 추출(외/내/폭) | 외경 | 내경 | 폭 |
|---|---|---|---|---|---|
| T1 | 360/190/200 | —/**190**/250 | ✗ | **✓** | ✗ |
| T2 | 380/190/200 | —/**190**/250 | ✗ | **✓** | ✗ |
| T3 | 380/260/? | 700/**260**/310 | ✗ | **✓** | ? |
| T4 | 420/260/? | **420**/**260**/310 | **✓** | **✓** | ? |
| T5 | 1036/580/200 | —/260/297 | ✗ | ✗ | ✗ |
| T8 | 500/260/200 | —/**260**/297 | ✗ | **✓** | ✗ |
| Thrust | 515/440/48 | 920/—/450 | ✗ | ✗ | ✗ |

**외경 1/7 | 내경 5/7 (71%) | 폭 0/5**

### 비교

| 분류 방법 | 외경 | 내경 | 폭 |
|---|---|---|---|
| K geometry (보정 전) | 0/7 | 0/7 | 0/5 |
| K geometry + 세션명 힌트 | 1/7 | 0/7 | 0/5 |
| SECTION 크롭 + 단순 크기 | 1/7 | 1/7 | 1/5 |
| **SECTION 크롭 + 위치 기반** | **1/7** | **5/7 (71%)** | **0/5** |

### 남은 과제
1. **외경**: Ø 기호 OCR 인식 개선 또는 세로 방향 텍스트 분류 강화
2. **폭**: 상단/하단 구분 로직 개선
3. **T5**: 외경 Ø1036이 SECTION 크롭 영역 밖에 있을 가능성
4. **Thrust**: 구조가 저널 베어링과 다름 — 별도 분류 필요
